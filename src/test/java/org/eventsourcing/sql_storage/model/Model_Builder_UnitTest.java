package org.eventsourcing.sql_storage.model;

import org.junit.Test;

public class Model_Builder_UnitTest {

    @Test
    public void test() {

        // Model model2 = new Model.Builder()
        // .type((t) -> t.name("ClassA")
        // .attrubute((a) -> a.name("attr_a").type(INTEGER))
        // .attrubute((a) -> a.name("attr_b").type(REFERENCE, MAP)
        // .relation((r) -> r.target("ClassB").reverse("attr_r"))
        // .relation((r) -> r.target("ClassC").reverse("attr_s")))
        // .attrubute("attr_c", TEXT))
        // .type((t) -> t.name("ClassB")
        // .attrubute((a) -> a.name("attr_r").type(REFERENCE, LIST)
        // .relation((r) -> r.target("ClassA").reverse("attr_b"))))
        // .type("ClassC", (t) -> t
        // .attrubute("attr_s", REFERENCE, LIST, (a) -> a
        // .relation("ClassA", "attr_b")))
        // .build();

    }

}
